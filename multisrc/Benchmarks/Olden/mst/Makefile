LEVEL = ../../..

PROG       = mst
CPPFLAGS   = -DTORONTO
LDFLAGS    = 
ifdef LARGE_PROBLEM_SIZE
RUN_OPTIONS = 4000
else
RUN_OPTIONS = 4000
endif

include $(LEVEL)/Makefile.multisrc
include $(LEVEL)/Makefile.rds

