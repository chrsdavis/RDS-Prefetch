LEVEL = ../../..
PROG = ft
CFLAGS += -std=gnu89 -fcommon
ifdef LARGE_PROBLEM_SIZE
RUN_OPTIONS += 6000 100000
else
RUN_OPTIONS +=  6000 100000
endif
HASH_PROGRAM_OUTPUT = 1
include $(LEVEL)/Makefile.multisrc
include $(LEVEL)/Makefile.rds

